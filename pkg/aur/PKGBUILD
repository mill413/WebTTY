# Maintainer: MebTTY Contributors
# This PKGBUILD is auto-generated by CI. Do not edit manually.

pkgname=mebtty
pkgver=${VERSION}
pkgrel=1
pkgdesc="A self-hosted web terminal that runs in your browser"
arch=('x86_64')
url="https://github.com/mill413/mebtty"
license=('MIT')
depends=('glibc' 'gcc-libs')
makedepends=()
provides=('mebtty')
conflicts=()
install=mebtty.install
source=("mebtty-${pkgver}-linux-amd64::${url}/releases/download/v${pkgver}/mebtty-${pkgver}-linux-amd64"
        "mebtty.service::${url}/raw/v${pkgver}/mebtty.service")
sha256sums=('SKIP'
            'SKIP')

package() {
    # Install executable
    install -Dm755 "mebtty-${pkgver}-linux-amd64" "$pkgdir/usr/bin/mebtty"

    # Install systemd service
    install -Dm644 "mebtty.service" "$pkgdir/usr/lib/systemd/system/mebtty.service"
}
