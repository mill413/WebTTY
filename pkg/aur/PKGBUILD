# Maintainer: WebTTY Contributors
# This PKGBUILD is auto-generated by CI. Do not edit manually.

pkgname=webtty
pkgver=${VERSION}
pkgrel=1
pkgdesc="A self-hosted web terminal that runs in your browser"
arch=('x86_64')
url="https://github.com/anthropics/webtty"
license=('MIT')
depends=('glibc' 'gcc-libs')
makedepends=()
provides=('webtty')
conflicts=()
install=webtty.install
source=("webtty-${pkgver}-linux-amd64::${url}/releases/download/v${pkgver}/webtty-${pkgver}-linux-amd64"
        "webtty.service::${url}/raw/v${pkgver}/webtty.service")
sha256sums=('SKIP'
            'SKIP')

package() {
    # Install executable
    install -Dm755 "webtty-${pkgver}-linux-amd64" "$pkgdir/usr/bin/webtty"

    # Install systemd service
    install -Dm644 "webtty.service" "$pkgdir/usr/lib/systemd/system/webtty.service"
}
